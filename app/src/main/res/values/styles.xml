<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="AppTheme.StatusBar" parent="AppTheme" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupInline">
        <item name="android:textColorPrimary">@color/md_white_1000</item>
    </style>

    <style name="CollapsingTabLayout">
        <item name="tabIndicatorColor">@color/md_white_1000</item>
        <item name="tabSelectedTextColor">@color/md_white_1000</item>
        <item name="tabTextColor">@color/md_grey_400</item>
    </style>
</resources>
